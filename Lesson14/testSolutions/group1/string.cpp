#include <iostream>
using namespace std;
double avgWordLength(char str[]) {
    double wordCount = 0;
    double letterCount = 0;

    for(int i = 0; str[i] != '\0'; i++) {
        
    }
}

int main() {
    char str[1024];
    cin.getline(str, 1024);
    cout << avgWordLength(str) << endl;
}